function monthOfyear(month) {
  switch (month) {
    case 1:
      console.log(`${month} 1 - january`);
      console.log(`${month} 2- february`);
      console.log(`${month} 12- december`);
      break;
    default:
        console.log(`${month} name of the month`);
      break;
    }
    switch (month) {
        case 2:
            console.log(`${month} 1- january`);
            console.log(`${month} 2- february`);
            console.log(`${month}12 - december`);
            break;
    
        default:
            console.log(` ${month}name of month`);
            break;
    }
    switch (month) {
        case 3:
            console.log(`${month} 1 - january`);
            console.log(`${month} 2- february`);
            console.log(`${month} 12 - december`);
            break;
    
        default:
            console.log(`${month} name of month`);
            break;
    }
    switch (month) {
        case 4:
            console.log(`${month} 1- january`);
            console.log(`${month} 2-february`);
            console.log(`${month} 12- december`);
            break;
    
        default:
            console.log(`${month}name of the month`);
            break;
    }
    switch (month) {
        case 5:
            console.log(`${month} 1 - january`);
            console.log(`${month} 2- february`);
            console.log(`${month} 12- december`);
            break;
    
        default:
            console.log(`${month}name of the month`);
            break;
    }
    switch (month) {
        case 6:
            console.log(`${month} 1- january`);
            console.log(`${month} 2 - february`);
            console.log(`${month} 12- december`);
            break;
    
        default:
            console.log(`${month}name of the month`);
            break;
    }
    switch (month) {
        case 6:
            console.log(`${month} 1 - january`);
            console.log(`${month} 2 - february`);
            console.log(`${month} 12- december`);
            break;
    
        default:
            console.log(`${month}name of the month`);
            break;
    }
    switch (month) {
        case 7:
            console.log(`${month} 1-january`);
            console.log(`${month} 2-febuary`);
            console.log(`${month} 12-december`);
            break;
    
        default:
            console.log(`${month}name of the month`);
            break;
    }
    switch (month) {
        case 8:
            console.log(`${month} 1-january`);
            console.log(`${month} 2-february`);
            console.log(`${month} 12-december`);
            break;
    
        default:
            console.log(`${month}name of the month`);
            break;
    }
}
monthOfyear(0);
monthOfyear(2);
monthOfyear(5);
monthOfyear(12);
monthOfyear(15);
monthOfyear(100);
monthOfyear(null);
monthOfyear(undefined);
